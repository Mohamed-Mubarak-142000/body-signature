# The backend API this dashboard talks to (no direct database access here)
BACKEND_URL="http://localhost:3001"

# Auth.js — session for the dashboard's own login (staff only)
AUTH_SECRET="generate-with: npx auth secret"
NEXTAUTH_URL="http://localhost:3002"
