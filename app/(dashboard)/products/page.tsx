import { Placeholder } from "@/components/placeholder";

export default function ProductsPage() {
  return (
    <Placeholder
      title="Products"
      note="Waiting on the backend's product/category CRUD endpoints (BACKEND_PRD.md §4.6)."
    />
  );
}
