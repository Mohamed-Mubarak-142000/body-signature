export default function OverviewPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold">Overview</h1>
      <p className="mt-2 text-sm text-neutral-500">
        This is a scaffold — most modules below just prove the round trip to
        the backend works. Real dashboards (metrics, recent orders/bookings)
        come once those backend features are built.
      </p>
    </div>
  );
}
